Make sure to install dependencies:

```bash
pnpm install
```

Build the application for production:

```bash
pnpm build
```

Locally run the profilers:

```bash
pnpm stats
```
and then:
```bash
pnpm heap
```
